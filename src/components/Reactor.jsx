import{useRef,useState}from'react';import logo from'../assets/logo.jpg';
export default function Reactor({compact=false}){const ref=useRef(),[t,setT]=useState({x:0,y:0});return <div className={`reactor ${compact?'compact':''}`} ref={ref} onPointerMove={e=>{const r=ref.current.getBoundingClientRect();setT({x:((e.clientY-r.top)/r.height-.5)*-12,y:((e.clientX-r.left)/r.width-.5)*12})}} onPointerLeave={()=>setT({x:0,y:0})}><i className="orbit o1"/><i className="orbit o2"/><i className="orbit o3"/><div className="logo3d" style={{transform:`rotateX(${t.x}deg) rotateY(${t.y}deg)`}}><span className="bevel"/><img src={logo} alt="V Pilot animated shield"/><span className="scan"/></div><div className="reactorcopy"><b>5D INTEGRATED INTELLIGENCE</b><small>Signals · Context · Authority · Action · Assurance</small></div></div>}
